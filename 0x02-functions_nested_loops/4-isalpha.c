#include "main.h"
 /**
  * _isalpha-print 1 if letter 0 if not
  *Return: 1 or 2
  */
int _isalpha(int c)
{
	if ( c = 'a' ; c <= 'z' ; c++)
	{	
		return (1);
	}
	else if (c = 'A' ; c <= 'Z' ; c++)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
