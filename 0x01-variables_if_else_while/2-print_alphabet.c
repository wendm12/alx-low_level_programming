/*alx software ingineering*/
#include<stdio.h>
/*main:print the alphabet in lowercase, followed by a new line */
int main(void)

{
	int ch;
	for (ch =  'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
		putchar('\n');
	return (0);
}

